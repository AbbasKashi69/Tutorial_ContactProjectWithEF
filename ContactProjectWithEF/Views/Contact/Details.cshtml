@using ContactProjectWithEF.Models.Entities
@model Contact

@{
}

<div>
    <div class="form-group">
        <label asp-for="FullName">نام</label>
        <label asp-for="FullName">@Model.FullName</label>
    </div>

    <div class="form-group">
        <label asp-for="Mobile">موبایل</label>
        <label asp-for="Mobile">@Model.Mobile</label>
    </div>

    <div class="form-group">
        <label asp-for="Phone">تلفن</label>
        <label asp-for="Phone">@Model.Phone</label>
    </div>

    <a href="/contact/index" class="btn btn-secondary">بازگشت</a>
</div>